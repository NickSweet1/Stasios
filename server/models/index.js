const Sub = require('./sub');
const User = require('./user');
const Contact = require('./contact');
const Coffee = require('./coffee');
const Desserts = require('./desserts');

module.exports = { Sub, User, Contact, Coffee, Desserts }